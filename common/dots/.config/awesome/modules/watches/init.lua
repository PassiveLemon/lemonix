return {
  cpu = require("modules.watches.cpu"),
  gpu = require("modules.watches.gpu"),
  memory = require("modules.watches.memory"),
  network = require("modules.watches.network"),
  other = require("modules.watches.other"),
  storage = require("modules.watches.storage"),
}
