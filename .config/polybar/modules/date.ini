[module/date]
type = internal/date
interval = 360

date = %a %h %e

format = <label>
format-prefix = "%{T3}%{T-}"
format-prefix-foreground = ${colors.dMag}

label = " %date%%"
label-font = 0
label-foreground = ${colors.dWhi}
